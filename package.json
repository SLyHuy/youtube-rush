{
  "name": "youtube-rush",
  "version": "3.0.0",
  "description": "**YouTube Rush** is a lightweight web extension designed to subtly improve the YouTube interface for long-form content and live streams. It automatically applies CSS tweaks to clean up distracting UI elements and enhance viewer focus—especially in fullscreen mode.",
  "files": [
    "manifest.json",
    "css/",
    "icons/",
    "popup/",
    "js/"
  ],
  "scripts": {
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:js": "eslint \"js/**/*.js\" \"popup/**/*.js\"",
    "lint": "npm run lint:css && npm run lint:js",
    "pack": "npm run lint && node -e \"const v=require('./manifest.json').version; const {execSync}=require('child_process'); const f='youtube-rush-v'+v+'.zip'; try{require('fs').unlinkSync(f)}catch(e){} execSync('zip -r '+f+' manifest.json css/ icons/ popup/ js/', {stdio:'inherit'}); console.log('Created '+f);\"",
    "prepare": "husky"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SLyHuy/youtube-rush.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/SLyHuy/youtube-rush/issues"
  },
  "homepage": "https://github.com/SLyHuy/youtube-rush#readme",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.2",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "stylelint": "^17.4.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
